//for vilde in main.c
#include "wave.h"

wave_mixer_t mixer={
  .wave={.spec={.rate=44100,
		.depth=16,
		.channels=1}},
  .spec={.rate=44100,
	 .depth=16,
	 .channels=1},
  .allocated = 0,
  .offset = 0
};
